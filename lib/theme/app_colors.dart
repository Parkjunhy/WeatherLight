import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color(0xFFF6F7FB);
  static const card = Colors.white;
  static const navy = Color(0xFF17233E);
  static const text = Color(0xFF1C2430);
  static const subText = Color(0xFF7D8796);

  // Accent color (requested: unify to time-setting blue tone)
  static const accent = Color(0xFF1C6BFF);
  static const blue = Color(0xFF247CFF);
  static const green = Color(0xFF00B894);

  static const shadowDark = Color(0x1A23324A);
  static const shadowLight = Color(0xB3FFFFFF);
}

